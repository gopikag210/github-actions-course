name: First_workflow
on: 
    workflow_dispatch:
jobs:
    first-job:
       runs-on: ubuntu-latest
       steps:
          - name: print welcome message
            run:  echo "welcome to first workflow"
            